<header>
    <h3 class="title">Members <b><i class="only">Only</i></b></h3>
    <nav class="welcome-back">
        <%if(!is_member) {%>
        <h4>Welcome back <%=currentUser.full_name %> !</h4>
        <h4 class="membership">Become a member?</h4>
        <%} else {%>
            <h4>Welcome to the club <%=currentUser.full_name %> !</h4>
            <%}%>
        <h4 class="sign-out"><a href="sign-out" style=" text-decoration: none;">Sign Out</a></h4>
    </nav>
</header>